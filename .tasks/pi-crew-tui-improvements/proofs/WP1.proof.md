# Package Proof: WP1 — Command overlay access, active-session scope, footer removal, and Escape safety

## Package Scope
Implemented WP1 access/safety shell only: `/subagents`, exact `/tasks` alias, session-wide active overlay shell, no-active notification, active-to-empty lifecycle, footer shortcut removal/hints, targeted Escape safety, cleanup/reopen, and user docs. WP2 split/narrow presentation polish remains out of scope.

## Assigned Slice Scope
- `.planning/pi-crew-tui-improvements/slices/current-state-and-constraints.md`
  - Must satisfy: `CREW-TUI-001`, `CREW-TUI-002`, `CREW-TUI-003`
- `.planning/pi-crew-tui-improvements/slices/live-details-access.md`
  - Must satisfy: `DETAILS-ACCESS-003`, `DETAILS-ACCESS-004`, `DETAILS-ACCESS-006`, `DETAILS-ACCESS-007`, `DETAILS-ACCESS-008`, `DETAILS-ACCESS-009`, `DETAILS-ACCESS-010`
  - Context only read/respected: `DETAILS-ACCESS-001`, `DETAILS-ACCESS-002`, `DETAILS-ACCESS-005`, `DETAILS-ACCESS-011`

## Implementation Strategy
Preserved existing widget/panel/interrupt seams instead of adding a new status surface. The command seam now owns explicit overlay access and initial empty handling; `SubagentsPanel` remains the focused component for navigation, details, and `d kill`; state watcher remains the live update source; interrupt handling owns only ambient Ctrl+B/C/Escape. Trust boundaries remain state-file driven and rendered text continues through existing active filtering/control-sequence stripping/width truncation. Escape safety is implemented by an overlay-active guard plus scoped double-Escape warning state, with one fresh state reload before destructive abort.

## Slice Closure Table

| Slice ID | Required understanding | Implementation evidence | Verification evidence | Status |
|---|---|---|---|---|
| `CREW-TUI-001` | Reconcile existing widget, footer, details command path, state watcher, and interrupts. | Retained above-editor widget and state-watcher fanout (`src/index.ts:253`); command path changed in place (`src/commands/tree.ts:8`); footer became non-interactive status/hint only (`src/ui/footer.ts:17`); interrupt logic updated in place (`src/ui/interrupt.ts:34`). | Targeted tests passed. Footer tests prove no ambient input registration and fresh status hint (`test/unit/footer.test.ts:90`); overlay tests prove command/lifecycle (`test/unit/overlay.test.ts:82`); interrupt tests prove Escape/Ctrl behavior (`test/unit/interrupt.test.ts:58`). | PASS |
| `CREW-TUI-002` | Use pi extension/TUI component APIs, repo namespace, themed/width-safe, cancellable overlay. | Overlay uses `ctx.ui.custom(..., { overlay: true, overlayOptions })` (`src/ui/overlay.ts:68`); `SubagentsPanel` remains themed component with Escape/Left close/back (`src/ui/subagents-panel.ts:53`); renderer preserves width-safe rows (`src/ui/subagents-panel-render.ts:31`). | Overlay API assertion passed (`test/unit/overlay.test.ts:115`); render width/control-sanitization tests passed (`test/unit/overlay.test.ts:266`, `test/unit/overlay.test.ts:321`). | PASS |
| `CREW-TUI-003` | Address known Escape/interrupt risk. | Ambient Escape now consumes with all-active session fallback warning, tracks warning scope, refreshes once before abort, and defers while overlay active (`src/ui/interrupt.ts:53`, `src/ui/interrupt.ts:82`). | Interrupt tests cover current-batch, timeout, older/unbatched/missing batch, fresh reload, fallback guard, overlay precedence, Ctrl+B/C unchanged (`test/unit/interrupt.test.ts:58`, `test/unit/interrupt.test.ts:123`, `test/unit/interrupt.test.ts:161`, `test/unit/interrupt.test.ts:304`). | PASS |
| `DETAILS-ACCESS-003` | Preserve editor input safety for explicit access. | Footer no longer registers terminal input or opens/focuses details from editor surface (`src/ui/footer.ts:17`); details require slash command (`src/commands/tree.ts:19`). | Footer test proves no input listener/setWidget in editor-empty/nonempty-capable context (`test/unit/footer.test.ts:90`); panel-internal Up/Down remains (`test/unit/footer.test.ts:116`). | PASS |
| `DETAILS-ACCESS-004` | Remove ambient Down-arrow details access, preserve in-panel navigation. | Removed footer Down/Enter focus/display state and below-editor widget path (`src/ui/footer.ts:17`); `SubagentsPanel` still handles Up/Down (`src/ui/subagents-panel.ts:65`). | Footer tests passed for no ambient registration and intact panel Up/Down (`test/unit/footer.test.ts:90`, `test/unit/footer.test.ts:116`). | PASS |
| `DETAILS-ACCESS-006` | Command-based first pass, no new global shortcut. | Registered `/subagents` and `/tasks`; no `registerShortcut` added. Footer hint advertises `/subagents`, not a key chord (`src/commands/tree.ts:19`, `src/ui/footer.ts:25`). | Command alias test passed (`test/unit/overlay.test.ts:82`); static grep found no `registerShortcut` in `src` and only interrupt `onTerminalInput`. | PASS |
| `DETAILS-ACCESS-007` | `/subagents` opens floating overlay. | `openSubagentsOverlay` calls `ctx.ui.custom<void>` with `overlay: true` and centered sizing (`src/ui/overlay.ts:50`, `src/ui/overlay.ts:95`); `/tasks` routes to same handler (`src/commands/tree.ts:23`). | Overlay mount test asserts `overlay: true`, not below-editor options, and Escape close (`test/unit/overlay.test.ts:115`). | PASS |
| `DETAILS-ACCESS-008` | Overlay shows all active current-session agents with current-batch-first ordering. | Added `filterSessionActiveStates`/`sortSessionActiveStates` including all `starting`/`running`, current batch rank first, then start time; labels older/unbatched in renderer (`src/ui/overlay.ts:36`, `src/ui/subagents-panel-render.ts:208`). | Filtering/render test covers current, older, unbatched, starting/running, terminal exclusion, labels, and contrasts current-batch-only helper (`test/unit/overlay.test.ts:136`). | PASS |
| `DETAILS-ACCESS-009` | Targeted Escape safety without Ctrl+B/C redesign. | Overlay-active guard defers ambient Escape (`src/ui/interrupt.ts:53`); scoped warning and fresh abort target policy (`src/ui/interrupt.ts:82`); Ctrl+B/C branches retained (`src/ui/interrupt.ts:40`, `src/ui/interrupt.ts:46`). | Tests passed for single warning, double abort, all-session fallback only after all-session warning, no-current-batch, fresh reload, overlay not arming double Escape, Ctrl+B/C unchanged (`test/unit/interrupt.test.ts:58`, `test/unit/interrupt.test.ts:123`, `test/unit/interrupt.test.ts:161`, `test/unit/interrupt.test.ts:232`, `test/unit/interrupt.test.ts:290`, `test/unit/interrupt.test.ts:313`, `test/unit/interrupt.test.ts:340`). | PASS |
| `DETAILS-ACCESS-010` | Initial empty state is notification, not overlay. | Command loads current-session states before mounting and calls exact info notification when none active (`src/ui/overlay.ts:57`, `src/ui/overlay.ts:59`). | No-active test passed: exact `No active sub-agents in this session.` info and no `custom`/watcher (`test/unit/overlay.test.ts:94`). | PASS |

## Acceptance / Verification Closure

| Expectation | Evidence | Status |
|---|---|---|
| Add or update targeted tests proving `/subagents` is registered, `/tasks` invokes the same handler and scope, the command path uses `ctx.ui.custom` with `overlay: true`, and the old command path is not below-editor-only. | VE-1: `/subagents` and `/tasks` share the same handler (`src/commands/tree.ts:8`); test asserts same handler and overlay custom options (`test/unit/overlay.test.ts:82`, `test/unit/overlay.test.ts:115`). | PASS |
| Add or update state-filtering tests with current-batch, older-batch, unbatched, starting/running, and terminal states proving the overlay includes all active current-session agents, excludes terminal states, sorts current-batch agents first, sorts other active agents by start time, labels non-current/unbatched agents when needed for clarity, and does not use the old current-batch-only default. | VE-2: `filterSessionActiveStates` and panel `currentBatchId` sorting implemented (`src/ui/overlay.ts:36`, `src/ui/subagents-panel.ts:39`); test covers required mix and labels (`test/unit/overlay.test.ts:136`). | PASS |
| Add or update command tests proving the exact info notification `No active sub-agents in this session.` is emitted and no overlay is mounted when no active agents exist at invocation. | VE-3: `NO_ACTIVE_SUBAGENTS_MESSAGE` and pre-mount branch (`src/ui/overlay.ts:13`, `src/ui/overlay.ts:59`); test asserts exact notify/no custom/no watcher (`test/unit/overlay.test.ts:94`). | PASS |
| Add or update overlay lifecycle tests proving active-to-empty updates keep the existing overlay mounted with an in-overlay empty state until Escape closes it, killable active agents still use `d kill` confirmation and the shared abort path, and closing/launching it again disposes or unsubscribes watchers/input handlers so closed overlays do not capture input and repeated launches do not create duplicate handlers. | VE-4: Watcher updates panel in place and cleanup in `finally` (`src/ui/overlay.ts:88`, `src/ui/overlay.ts:100`); `d kill` remains selected+confirmed (`src/ui/subagents-panel.ts:71`, `src/ui/subagents-panel.ts:104`). Tests passed (`test/unit/overlay.test.ts:167`, `test/unit/overlay.test.ts:190`, `test/unit/overlay.test.ts:213`, `test/unit/overlay.test.ts:242`). | PASS |
| Add or update footer/widget/state-watcher tests or static inspections proving ambient Down and Enter are not consumed to focus or display pi-crew details from the editor/footer surface, including editor-empty and editor-nonempty cases, while panel-internal Up/Down remains outside this removal; also prove the ambient active-agents summary remains visible or is intentionally replaced, the footer/status hint advertises `/subagents · Ctrl+B background · Esc Esc abort` or an equivalently concise implemented hint, and both ambient surfaces update from the live state-watcher/session-state path without stale counts/status across agent start, finish, overlay launch, and overlay close. | VE-5: Footer has no input listener and status hint includes `/subagents · Ctrl+B background · Esc Esc abort` (`src/ui/footer.ts:17`, `src/ui/footer.ts:25`); state watcher still updates widget/footer/interrupt (`src/index.ts:253`); widget retained (`src/ui/widget.ts:23`). Tests/static inspection passed (`test/unit/footer.test.ts:68`, `test/unit/footer.test.ts:90`, `test/unit/footer.test.ts:116`; grep command below). | PASS |
| Add or update Escape/interrupt tests reproducing the single-Escape risk: overlay Escape closes/backs out before ambient interrupt handling, does not reach parent abort or sub-agent kill, and does not arm ambient double-Escape state; the next ambient Escape after closing is treated as a first warning; ambient single Escape with any active current-session sub-agent warns with target scope/count and consumes; double Escape within the configured window refreshes state once and aborts current-batch active agents when present, otherwise all active current-session agents only after a matching all-active warning; stale watcher, no-current-batch, missing `batchId`, older-batch, and unbatched active cases are covered; Ctrl+B and Ctrl+C behavior remains unchanged. | VE-6: Interrupt implementation evidence in `src/ui/interrupt.ts:34-132`; tests cover all listed scenarios (`test/unit/interrupt.test.ts:58`, `test/unit/interrupt.test.ts:123`, `test/unit/interrupt.test.ts:161`, `test/unit/interrupt.test.ts:197`, `test/unit/interrupt.test.ts:232`, `test/unit/interrupt.test.ts:290`, `test/unit/interrupt.test.ts:313`, `test/unit/interrupt.test.ts:340`). | PASS |
| Statically inspect command and input registration to prove no new global shortcut or hidden details-opening key was added, Ctrl+C is not treated as overlay close, overlay Escape cannot be preempted by the ambient interrupt listener, overlay close cleans up listeners/watchers, and user-visible hints advertise `/subagents` rather than a key chord. | VE-7: Static inspection command showed no `registerShortcut`, no overlay below-editor path, only interrupt raw input, and footer/docs hints advertise `/subagents`. `SubagentsPanel` returns false for Ctrl+C (`src/ui/subagents-panel.ts:54`), so Ctrl+C is not close. | PASS |
| Update README or user-facing docs/hints so `/subagents`, `/tasks` compatibility, live session-wide scope, and cancellation wording are accurate and audience-facing text contains no Super Developer planning/package terminology. | VE-8: README command/cancellation docs updated (`README.md:16`, `README.md:47`); bug note updated with implemented fix (`docs/bugs/single-escape-kills-subagents.md:74`). | PASS |
| Run `npm test -- test/unit/footer.test.ts test/unit/interrupt.test.ts test/unit/overlay.test.ts` and `npm run typecheck`, or document an equivalent narrower command only if it exercises the same changed surfaces. | VE-9: Required test command passed: 3 files, 30 tests. `npm run typecheck` passed. | PASS |
| Package verification must map each expectation to `VE-<n>` rows and must inspect package scope, assigned Slices, changed code/diff, tests, UI text, ambient summary/footer state freshness, and known input-precedence risks; planner-provided seeds do not limit verifier discovery. | VE-10: This proof maps package expectations to VE-1..VE-10 and records code/test/docs/static evidence. Report path intentionally not created by package agent. | PASS |

## Commands Run
- `npm test -- test/unit/footer.test.ts test/unit/interrupt.test.ts test/unit/overlay.test.ts` (cwd: `/home/lenin/work-repos/pi-crew/.worktrees/pi-crew-tui-improvements/wp-WP1`) — exit 0; 3 test files, 30 tests passed.
- `npm run typecheck` (same cwd) — exit 0; `tsc --noEmit` passed.
- `grep -R "registerShortcut\|onTerminalInput\|belowEditor\|/subagents\|Ctrl+B background\|Esc Esc abort" -n src README.md docs/bugs/single-escape-kills-subagents.md` — exit 0; static inspection results recorded in VE-7.
- `git diff --check` — exit 0; no whitespace errors.
- `python3 /home/lenin/.pi/agent/claude-plugin-manager/cache/super-developer-marketplace/super-developer/1.33.1/assets/sliceproof.py validate-plan .tasks/pi-crew-tui-improvements/tasks.json` — exit 0; `ok: true`, validated packages WP1/WP2 and assigned slices.
- `python3 /home/lenin/.pi/agent/claude-plugin-manager/cache/super-developer-marketplace/super-developer/1.33.1/assets/sliceproof.py validate-proof .tasks/pi-crew-tui-improvements/tasks.json --package WP1` — exit 0; `ok: true` for WP1 proof.

## Files Changed / Inspected
Changed:
- `src/commands/tree.ts` — `/subagents` registration and exact `/tasks` alias.
- `src/ui/overlay.ts` — custom overlay access, no-active notification, all-active session filtering/sorting, overlay active marker, lifecycle cleanup.
- `src/ui/footer.ts` — removed ambient Down/Enter handling; retained status hint.
- `src/ui/interrupt.ts` — overlay Escape deferral and scoped double-Escape target policy.
- `src/ui/subagents-panel.ts` — current-batch-aware sorting and dispose hook.
- `src/ui/subagents-panel-render.ts` — in-overlay empty message and older/unbatched labels.
- `test/unit/footer.test.ts`, `test/unit/interrupt.test.ts`, `test/unit/overlay.test.ts` — targeted WP1 coverage.
- `README.md`, `docs/bugs/single-escape-kills-subagents.md` — user-facing docs.
- `.tasks/pi-crew-tui-improvements/packages/WP1.md`, `.tasks/pi-crew-tui-improvements/proofs/WP1.proof.md` — mechanical expectation wording refresh and proof synchronization.
Inspected:
- `src/index.ts` state-watcher fanout and mount order.
- `src/ui/widget.ts` ambient active-agents summary retained.
- Assigned package/spec/tasks/Slice files and package-agent/quality contracts.

## Gaps, Deviations, or Deferred Items
None.

## Package Agent Completion Statement
WP1 implementation is complete within assigned scope. Evidence above covers command aliasing, overlay API/lifecycle, active session filtering/order/labels, no-active behavior, footer shortcut removal/hints, targeted Escape safety, docs, static registration inspection, and required tests/typecheck. No new dependencies, external services, credentials, unsafe commands, or Slice plan defects were encountered. Mocks were limited to unit-test UI/watchers/handlers; state-store kill and command initial-load paths used real temp files where material. Proof rows and verification expectations are consistent with the implemented diff, and `sliceproof.py validate-proof` now passes for WP1.
